Documentation and workflow artifacts for the Amazon Bedrock Workflow.

- Moved YAML and text resources live in this folder.
- Use `components/`, `examples/`, `output/`, and `tests/` for other assets.
